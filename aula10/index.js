// Tipos de dados primitivos em JavaScript
// String = texto;
// Number = número;
// Boolean = true ou false / verdadeiro ou falso;

// String
const nome = 'Lucas';
const nome1 = 'Araújo';
const nome2 = 'Silva';

// Number
const num1 = 10;
const num2 = 10.52;

// undefined -> não aponta pra local nenhum na memória;
let nomeAluno; 

// Nulo -> não aponta pra local nenhum na memória. (Usamos quando queremos resertar o valor de uma variável);
const sobrenomeAluno = null;

// Boolean -> já inicia como falso;
const aprovado = false; // Booelan = true, false (lógico);

// Checar o tipo de variável
console.log(typeof nome, nome);